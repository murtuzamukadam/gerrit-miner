The Python script, GerritRev.py  makes use of the various
web methods and extracts the relevant data from the JSON
responses. A database schema based on the
information returned from the server is created and the data is stored in
a Microsoft SQL Server database. The data is made available as well.